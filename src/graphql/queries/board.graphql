query board($boardId: Int!) {
  board(boardId: $boardId) {
    id
    type
    content
    writerId
    writer {
      id
      username
      userId
      email
      createdAt
      updatedAt
    }
    views
    likes
    isLike
    reviews
    createDate
    modifiedDate
    isDeleted
  }
}
